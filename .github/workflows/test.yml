name: Test
on:
  workflow_dispatch: {}
jobs:
  shell-step:
    name: Shell Step
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Shell Step
        shell: bash
        run: echo "hello from shell"
